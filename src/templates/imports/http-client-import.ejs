<%
/**
* @description HttpClient 인터페이스 import 구문 생성 템플릿 입니다.
*/

const { codegenConfig, resolveRelativeImportPath } = it;

const { pathInfo } = codegenConfig.customOutput;
const httpClientAlias = resolveRelativeImportPath(pathInfo.httpClient.output.relative)
%>
import type { HttpClient } from '<%~ httpClientAlias %>';
